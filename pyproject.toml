[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pip-search-ex"
version = "2.0.7"
authors = [
    {name = "thedwarf", email = "python@dwarven.com"},
]
description = "Fast PyPI package search with unified search architecture, TUI interface, and 20+ themes. Part of the DwarvenSuite."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Utilities",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Environment :: Console",
    "Environment :: Console :: Curses",
]
keywords = ["pip", "search", "package", "pypi", "tui", "terminal", "cli"]
dependencies = [
    "requests>=2.25.0",
    "wcwidth>=0.2.0",
    "textual>=0.47.0",
    "packaging>=20.0",
]

[project.optional-dependencies]
dev = [
    "build",
    "twine",
]

[project.urls]
Homepage = "https://github.com/gitdwarf/pip-search-ex"
Repository = "https://github.com/gitdwarf/pip-search-ex"
"Bug Reports" = "https://github.com/gitdwarf/pip-search-ex/issues"

[project.scripts]
pip-search-ex = "pip_search_ex.p_s_e:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["pip_search_ex", "pip_search_ex.*"]
exclude = ["tests*", "__pycache__"]

[tool.setuptools.package-data]
"*" = ["themes/*.xml"]
